---
title: "Getting started with FastAPI"
date: 2026-02-18
category: Technical
---
# Introduction
I am hearing  a lot about another python web framework named "FastAPI". In this post let us understand the hype around it. The focus will be primarily on getting to know 
* What is FastAPI?
* When to use it?
* FastAPI vs DRF
* FastAPI Architecture
* FastAPI Core Components
* How to get started with a simple project

## What is FastAPI?
FastAPI is a Python web framework for building API's. Decorators (like @app.get("/<end_point>") are used to define endpoints, it uses Python type hints to automatically validate incoming data, convert types and generate interactive API documentation (at /docs) out of the box.

It is built on Starlette(for the web layer) and Pydantic(for data validation)

### What is Starlette?
Starlette is a lightweight python web framework build on ASGI (Asynchronous Server Gateway Interface) which is sucessor of WSGI (Web Server Gateway Interface). FastAPI is built on top of Starlette by adding automatic data validation, serialization and API docs to it.

### What is Pydantic?
Pydantic is a python library for data validation using type hints. Let me elaborate with an illustration for better understanding.

```
from pydantic import BaseModel

class Customer(BaseModel):
    name: str
    age: int
    email: str
    pincode: int

user = User(name="Alice", age=25, email="alice@example.com")  # works
user = User(name="Alice", age="not a number", email="...")    # validation error
```


The above class takes 3 parameters as input name as string,age an integer and email as string. If the user passes an invalid  data type like for e.g age="not a number" it will throw a validation error as it is expecting an integer. However, Pydantic does "type coercion". Let us consider the user passes age="45", the expectation is to throw a validation error as the parameter datatype is "string" and expected datatype is "int" but in reality due to the intervention of "type coercion" the string is automatically converted to integer and the execution continues.

FastAPI uses Pydantic under the hood so that when a request comes in, the JSON body is automatically parsed and validated against your model before your function even runs.

## When to choose FastAPI?
One should use FastAPI if speed, validation and clean code takes precedence. The best use cases would be:
* Building REST API which is high performant
* ML/AI services
* Auto generated documents matters
* Rapid prototyping

## FastAPI vs DRF
|Feature | FastAPI | DRF |
|---|---|---|
| Based on | Starlette+Pydantic | Django |
| Performance | Fast (async by default) | Slower (sync by default) |
| Object Relational Mapping | Not built-in | Django ORM (built-in and powerful)
| Admin Panel | No | Yes (Django admin)
| Authenciation & Permissions | Basic | Rich (built-in)

# FastAPI Architecture

![fastAPI_Architecture]({{ "/assets/images/fastapi/fastapi-architecture.png" | relative_url }})

# Core Components

* ASGI Server (Uvicorn)
  The server that runs the FastAPI app. It receives HTTP request from clients and sends responses back. Built on uvloop(fast async event loop) and httptools(fast HTTP parser)
  
* Starlette
  FastAPI directly subclasses Starlette inheriting all its capabilities. It handles the routing, middleware, request/response objects, websockets and static files.

* Pydantic
  This component is responsible for data validation. It powers all input validation and output serialization. Once BaseModel is defined with Python, type hints and FastAPI enforces it automatically — no extra code needed for it.
  
* OpenAPI & Auto Docs
  FastAPI auto-generates an OpenAPI schema from your code. Two interactive docs UIs are available out of the box — Swagger UI at /docs and ReDoc at /redoc - without any extra setup
  
* Dependency Injection (DI) System
  A clean system to share reusable logic across routes — database connections, authentication, configuration. FastAPI resolves dependencies automatically before calling your handler.
  #### Dependancy Injection example?
  ```
  from fastapi import FastAPI, Depends

  app = FastAPI()

  # --- Dependency function ---
  def get_db():
    db = connect_to_database()
    try:
        yield db        # provides the db to the route
    finally:
        db.close()      # runs after the route finishes

  # --- Route using the dependency ---
  @app.get("/items")
  def read_items(db = **Depends**(get_db)):
    return db.query("SELECT * FROM items")
  ```
  Steps:
  1) Define a dependency function (for e.g., def get_db which return database handle)
  2) Call the dependency function name in the route using "Depends" (Depends(get_db)
     
  #### Dependancy Injection vs Direct Call
  Looking at the above example, one may argue that one can use the direct call of the method (e.g., get_db()) why use dependancy injection? On the surface level it looks similar but it does more than a direct call. Below are some points that make it different:
  1. LifeCycle Management
     With a direct call, you have to manage setup and teardown yourself:
     ```
     python# Direct — you manage everything
     @app.get("/items")
     def read_items():
        db = get_db()
        try:
            result = db.query(...)
            return result
        finally:
            db.close()   # easy to forget!
     ```
     With Depends(), the dependency handles it:
     ```
     def get_db():
        db = connect()
        try:
            yield db
        finally:
            db.close()   # always runs, even on errors

     @app.get("/items")
     def read_items(db = Depends(get_db)):
        return db.query(...)   # clean, nothing to manage
     ```
  2. Caching within a request
     If multiple routes need the same method, Depends() calls it only once per request and reuses the result but with direct call the method is called multiple times within the same request.
     
  3. Automatic parameter injection
     Dependencies can read query params, headers, and path params directly from the request — without you passing anything but with a direct call, you'd have to extract the header manually in every route.
     
* Middleware
  Functions that run on every request before it reaches your route handler, and on every response before it goes back to the client. Common uses: CORS, logging, authentication, GZip compression. 
  

# Fast API Workflow
Request → Uvicorn 
        → Starlette (routing + middleware)
        → Pydantic (validate input)
        → Your handler + Depends()
        → Pydantic (serialize output)
        → Starlette (wrap response)
→ Uvicorn → Client
  
# Getting Started

## Prerequisite
* Python 3.10 or higher

## Install fastapi module
```

```

